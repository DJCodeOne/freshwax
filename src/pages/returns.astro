---
// src/pages/returns.astro
// SSR to avoid Astro.request.headers build warning
export const prerender = false;

// Redirect to shipping page which now contains returns information
return Astro.redirect('/shipping#returns-refunds', 301);
---
