---
// src/pages/merch.astro
// Public Merch Store - Cloudflare Pages compatible (uses REST API)

import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FloatingShuffleButton from '../components/FloatingShuffleButton.astro';
import { queryCollection } from '../lib/firebase-rest';
import '../styles/merch.css';

export const prerender = false;

// Fetch merch using REST API (no Admin SDK)
let products: any[] = [];
let categories: Record<string, string> = {};
let productTypes: Record<string, string> = {};

try {
  // Fetch all merch and filter locally (composite filters may not work without indexes)
  let merchData = await queryCollection('merch', {
    cacheKey: 'merch-all',
    cacheTTL: 10 * 60 * 1000 // 10 min cache
  });

  // Filter to only published/active items locally
  merchData = merchData.filter(item => {
    // Check for active/published items
    const isPublished = item.published !== false;
    const isActive = !item.status || item.status === 'active';
    return isPublished && isActive;
  });
  
  products = merchData.map(doc => {
    // Build filter options
    if (doc.category && doc.categoryName) {
      categories[doc.category] = doc.categoryName;
    }
    if (doc.productType && doc.productTypeName) {
      productTypes[doc.productType] = doc.productTypeName;
    }
    
    // Calculate stock status
    const stock = doc.totalStock || 0;
    const threshold = doc.lowStockThreshold || 5;
    let stockStatus = 'in-stock';
    let stockLabel = 'In Stock';
    
    if (stock === 0) {
      stockStatus = 'out-of-stock';
      stockLabel = 'Out of Stock';
    } else if (stock <= threshold) {
      stockStatus = 'low-stock';
      stockLabel = 'Low Stock';
    }
    
    // Get images - images array contains objects with url property
    let allImages: string[] = [];
    if (doc.images && Array.isArray(doc.images)) {
      allImages = doc.images
        .map((img: any) => {
          if (typeof img === 'string') return img;
          if (img && typeof img === 'object' && img.url) return img.url;
          return null;
        })
        .filter(Boolean);
    }
    
    if (allImages.length === 0) {
      if (doc.primaryImage) allImages = [doc.primaryImage];
      else if (doc.imageUrl) allImages = [doc.imageUrl];
    }
    
    const imageUrl = doc.primaryImage || allImages[0] || '';
    
    // Process colors - array of {hex, name} objects
    let colorList: string[] = [];
    let colorNames: string[] = [];
    if (doc.colors && Array.isArray(doc.colors)) {
      colorList = doc.colors.map((c: any) => c.hex || c).filter(Boolean);
      colorNames = doc.colors.map((c: any) => c.name || c.hex || c).filter(Boolean);
    }
    
    return {
      ...doc,
      stockStatus,
      stockLabel,
      images: allImages,
      imageUrl,
      colorList,
      colorNames,
      retailPrice: parseFloat(doc.retailPrice) || 0,
      salePrice: doc.salePrice ? parseFloat(doc.salePrice) : null,
    };
  });
  
  // Sort: featured first, then by date
  products.sort((a, b) => {
    if (a.featured && !b.featured) return -1;
    if (!a.featured && b.featured) return 1;
    return new Date(b.createdAt || 0).getTime() - new Date(a.createdAt || 0).getTime();
  });
  
} catch (e) {
  console.error('[Merch] Error fetching:', e);
}

const onSaleCount = products.filter(p => p.onSale).length;
const productsJson = JSON.stringify(products);
---

<Layout title="Merch Store - Fresh Wax">
  <Header />

  <main class="merch-page">
    <!-- Hero Section -->
    <section class="merch-hero">
      <div class="hero-container">
        <h1 class="hero-title">MERCH <span class="red">STORE</span></h1>
        <p class="hero-subtitle">Sound Systems, Crews, Labels, DJs & More</p>
      </div>
    </section>

    <!-- Content Section -->
    <div class="merch-content">
      <!-- Filters Bar - Full Width -->
      <section class="filters-bar">
        <div class="filters-row">
          <div class="filter-item">
            <label>Category</label>
            <select id="categoryFilter">
              <option value="">All</option>
              {Object.entries(categories).map(([key, name]) => (
                <option value={key}>{name}</option>
              ))}
            </select>
          </div>

          <div class="filter-item">
            <label>Type</label>
            <select id="typeFilter">
              <option value="">All</option>
              {Object.entries(productTypes).map(([key, name]) => (
                <option value={key}>{name}</option>
              ))}
            </select>
          </div>

          <div class="filter-item">
            <label>Availability</label>
            <select id="stockFilter">
              <option value="">All</option>
              <option value="in-stock">In Stock</option>
              {onSaleCount > 0 && <option value="on-sale">On Sale</option>}
            </select>
          </div>

          <div class="filter-item">
            <label>Sort By</label>
            <select id="sortSelect">
              <option value="featured">Featured</option>
              <option value="newest">Newest</option>
              <option value="price-low">Price ↑</option>
              <option value="price-high">Price ↓</option>
              <option value="name">A-Z</option>
            </select>
          </div>

          <button id="clearFilters" class="clear-btn">Clear</button>
          
          <div class="products-count">
            <span id="visibleCount">{products.length}</span> / {products.length}
          </div>
        </div>
      </section>

      <!-- Products Grid - 2x2 -->
      <section class="products-section">
        {products.length === 0 ? (
          <div class="empty-state">
            <span>👕</span>
            <h3>No merch available</h3>
            <p>Check back soon!</p>
          </div>
        ) : (
          <div id="productsGrid" class="products-grid">
            {products.map((product) => (
              <div 
                class="product-card"
                data-id={product.id}
                data-category={product.category || ''}
                data-type={product.productType || ''}
                data-stock={product.stockStatus}
                data-sale={product.onSale ? 'true' : 'false'}
                data-featured={product.featured ? 'true' : 'false'}
                data-price={product.salePrice || product.retailPrice}
                data-name={product.name}
                data-created={product.createdAt || ''}
                data-product={JSON.stringify(product)}
              >
                <!-- Image -->
                <div class="card-image card-clickable">
                  {product.imageUrl ? (
                    <img src={product.imageUrl} alt={product.name} loading="lazy" />
                  ) : (
                    <div class="placeholder">👕</div>
                  )}
                  
                  <div class="badges">
                    {product.onSale && <span class="badge sale">SALE</span>}
                    {product.featured && <span class="badge featured">★</span>}
                    {product.stockStatus === 'out-of-stock' && <span class="badge sold-out">SOLD OUT</span>}
                  </div>
                </div>
                
                <!-- Info -->
                <div class="card-info card-clickable">
                  <span class="product-name">{product.name}</span>
                  <p class="product-meta">{product.categoryName}</p>
                  
                  {product.sizes && product.sizes.length > 0 && (
                    <p class="product-sizes">{product.sizes.join(' • ')}</p>
                  )}
                  
                  {product.colorList && product.colorList.length > 0 && (
                    <div class="color-dots">
                      {product.colorList.map((color: string) => (
                        <span class="dot" style={`background:${color}`}></span>
                      ))}
                    </div>
                  )}
                </div>
                
                <!-- Buy Button -->
                <button 
                  class="buy-btn" 
                  disabled={product.stockStatus === 'out-of-stock'}
                >
                  {product.stockStatus === 'out-of-stock' ? (
                    <span>Sold Out</span>
                  ) : (
                    <>
                      <span class="buy-text">BUY</span>
                      <span class="buy-price">
                        {product.onSale && product.salePrice ? (
                          <>£{product.salePrice.toFixed(2)}</>
                        ) : (
                          <>£{product.retailPrice.toFixed(2)}</>
                        )}
                      </span>
                    </>
                  )}
                </button>
              </div>
            ))}
          </div>
        )}
      </section>
    </div>
  </main>

  <!-- Quick View / Buy Modal -->
  <div id="buyModal" class="modal-overlay">
    <div class="modal-box">
      <button class="modal-close">&times;</button>
      
      <div class="modal-grid">
        <!-- Image -->
        <div class="modal-image">
          <img id="modalImg" src="" alt="" />
          <div id="modalThumbs" class="modal-thumbs"></div>
        </div>
        
        <!-- Details -->
        <div class="modal-info">
          <h2 id="modalName"></h2>
          <p id="modalCategory" class="modal-cat"></p>
          <p id="modalDesc" class="modal-desc"></p>
          
          <div id="modalPriceBox" class="modal-price"></div>
          
          <!-- Size Selection -->
          <div id="sizeSection" class="option-section">
            <label>Select Size <span class="required">*</span></label>
            <div id="sizeOptions" class="option-grid"></div>
          </div>
          
          <!-- Color Selection -->
          <div id="colorSection" class="option-section">
            <label>Select Colour <span class="required">*</span></label>
            <div id="colorOptions" class="option-grid"></div>
          </div>
          
          <!-- Stock Status -->
          <div id="modalStockStatus" class="stock-info"></div>
          
          <!-- Add to Cart -->
          <button id="addToCartBtn" class="add-to-cart-btn" disabled>
            <span class="btn-text">Select Options</span>
            <span class="btn-price"></span>
          </button>
          
          <a id="viewDetailsLink" href="" class="view-details-link">View Larger with Zoom →</a>
        </div>
      </div>
    </div>
  </div>

  <FloatingShuffleButton />
  <Footer />
</Layout>

<script define:vars={{ productsJson }}>
  window.PRODUCTS_DATA = JSON.parse(productsJson);
</script>

<script>
  function init() {
    // Filters
    const categoryFilter = document.getElementById('categoryFilter') as HTMLSelectElement;
    const typeFilter = document.getElementById('typeFilter') as HTMLSelectElement;
    const stockFilter = document.getElementById('stockFilter') as HTMLSelectElement;
    const sortSelect = document.getElementById('sortSelect') as HTMLSelectElement;
    const clearBtn = document.getElementById('clearFilters');
    const grid = document.getElementById('productsGrid');
    const countEl = document.getElementById('visibleCount');

    function applyFilters() {
      const category = categoryFilter?.value || '';
      const type = typeFilter?.value || '';
      const stock = stockFilter?.value || '';
      const sort = sortSelect?.value || 'featured';

      const cards = Array.from(grid?.querySelectorAll('.product-card') || []) as HTMLElement[];
      let visible = 0;

      cards.forEach(card => {
        let show = true;
        if (category && card.dataset.category !== category) show = false;
        if (type && card.dataset.type !== type) show = false;
        if (stock === 'in-stock' && card.dataset.stock === 'out-of-stock') show = false;
        if (stock === 'on-sale' && card.dataset.sale !== 'true') show = false;
        card.style.display = show ? '' : 'none';
        if (show) visible++;
      });

      const visibleCards = cards.filter(c => c.style.display !== 'none');
      visibleCards.sort((a, b) => {
        switch (sort) {
          case 'featured':
            if (a.dataset.featured === 'true' && b.dataset.featured !== 'true') return -1;
            if (a.dataset.featured !== 'true' && b.dataset.featured === 'true') return 1;
            return 0;
          case 'newest':
            return new Date(b.dataset.created || 0).getTime() - new Date(a.dataset.created || 0).getTime();
          case 'price-low':
            return parseFloat(a.dataset.price || '0') - parseFloat(b.dataset.price || '0');
          case 'price-high':
            return parseFloat(b.dataset.price || '0') - parseFloat(a.dataset.price || '0');
          case 'name':
            return (a.dataset.name || '').localeCompare(b.dataset.name || '');
          default:
            return 0;
        }
      });
      visibleCards.forEach(card => grid?.appendChild(card));
      if (countEl) countEl.textContent = String(visible);
    }

    categoryFilter?.addEventListener('change', applyFilters);
    typeFilter?.addEventListener('change', applyFilters);
    stockFilter?.addEventListener('change', applyFilters);
    sortSelect?.addEventListener('change', applyFilters);
    clearBtn?.addEventListener('click', () => {
      if (categoryFilter) categoryFilter.value = '';
      if (typeFilter) typeFilter.value = '';
      if (stockFilter) stockFilter.value = '';
      if (sortSelect) sortSelect.value = 'featured';
      applyFilters();
    });

    // Modal
    const modal = document.getElementById('buyModal');
    const modalClose = document.querySelector('.modal-close');
    let currentProduct: any = null;
    let selectedSize: string | null = null;
    let selectedColor: string | null = null;

    // Open modal when clicking on card image, info, or buy button
    document.querySelectorAll('.product-card').forEach(card => {
      const product = JSON.parse((card as HTMLElement).dataset.product || '{}');
      
      // Click on image or info opens modal
      card.querySelectorAll('.card-clickable').forEach(el => {
        el.addEventListener('click', (e) => {
          e.preventDefault();
          openModal(product);
        });
      });
      
      // Buy button also opens modal
      const buyBtn = card.querySelector('.buy-btn');
      buyBtn?.addEventListener('click', (e) => {
        e.preventDefault();
        openModal(product);
      });
    });

    modalClose?.addEventListener('click', closeModal);
    modal?.addEventListener('click', (e) => {
      if (e.target === modal) closeModal();
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') closeModal();
    });

    function openModal(product: any, skipPushState = false) {
      if (!modal) return;
      currentProduct = product;
      selectedSize = null;
      selectedColor = null;
      
      // Add hash to URL for back navigation (unless coming from popstate)
      if (!skipPushState) {
        history.pushState({ productId: product.id }, '', `#quick-view-${product.id}`);
      }

      // Image
      const mainImg = document.getElementById('modalImg') as HTMLImageElement;
      if (mainImg) {
        mainImg.src = product.imageUrl || '';
        mainImg.alt = product.name;
      }

      // Thumbnails
      const thumbs = document.getElementById('modalThumbs');
      if (thumbs && product.images?.length > 1) {
        thumbs.innerHTML = product.images.map((img: string, i: number) => `
          <button class="thumb ${i === 0 ? 'active' : ''}" data-src="${img}">
            <img src="${img}" alt="" />
          </button>
        `).join('');
        thumbs.querySelectorAll('.thumb').forEach(t => {
          t.addEventListener('click', () => {
            mainImg.src = (t as HTMLElement).dataset.src || '';
            thumbs.querySelectorAll('.thumb').forEach(x => x.classList.remove('active'));
            t.classList.add('active');
          });
        });
      } else if (thumbs) {
        thumbs.innerHTML = '';
      }

      // Details
      const nameEl = document.getElementById('modalName');
      const catEl = document.getElementById('modalCategory');
      const descEl = document.getElementById('modalDesc');
      const priceBox = document.getElementById('modalPriceBox');
      const viewLink = document.getElementById('viewDetailsLink') as HTMLAnchorElement;

      if (nameEl) nameEl.textContent = product.name || '';
      if (catEl) catEl.textContent = product.categoryName || '';
      if (descEl) descEl.textContent = product.description || '';
      if (viewLink) viewLink.href = `/merch/${product.id}`;

      if (priceBox) {
        const price = product.onSale && product.salePrice ? product.salePrice : product.retailPrice;
        if (product.onSale && product.salePrice) {
          priceBox.innerHTML = `
            <span class="current-price">£${product.salePrice.toFixed(2)}</span>
            <span class="old-price">£${product.retailPrice.toFixed(2)}</span>
            <span class="save-badge">SAVE £${(product.retailPrice - product.salePrice).toFixed(2)}</span>
          `;
        } else {
          priceBox.innerHTML = `<span class="current-price">£${product.retailPrice.toFixed(2)}</span>`;
        }
      }

      // Sizes
      const sizeSection = document.getElementById('sizeSection');
      const sizeOptions = document.getElementById('sizeOptions');
      if (sizeSection && sizeOptions) {
        if (product.sizes?.length > 0) {
          sizeSection.style.display = 'block';
          sizeOptions.innerHTML = product.sizes.map((s: string, i: number) => 
            `<button class="opt-btn size-btn ${product.sizes.length === 1 ? 'selected' : ''}" data-size="${s}">${s}</button>`
          ).join('');
          
          // Auto-select if only one size
          if (product.sizes.length === 1) {
            selectedSize = product.sizes[0];
          }
          
          sizeOptions.querySelectorAll('.size-btn').forEach(btn => {
            btn.addEventListener('click', () => {
              sizeOptions.querySelectorAll('.size-btn').forEach(b => b.classList.remove('selected'));
              btn.classList.add('selected');
              selectedSize = (btn as HTMLElement).dataset.size || null;
              updateCartButton();
            });
          });
        } else {
          sizeSection.style.display = 'none';
          selectedSize = 'none';
        }
      }

      // Colors
      const colorSection = document.getElementById('colorSection');
      const colorOptions = document.getElementById('colorOptions');
      if (colorSection && colorOptions) {
        if (product.colorList?.length > 0) {
          colorSection.style.display = 'block';
          colorOptions.innerHTML = product.colorList.map((hex: string, i: number) => {
            const name = product.colorNames?.[i] || hex;
            const isSelected = product.colorList.length === 1 ? 'selected' : '';
            return `<button class="opt-btn color-opt ${isSelected}" data-color="${name}" data-hex="${hex}" style="background:${hex}" title="${name}"></button>`;
          }).join('');
          
          // Auto-select if only one color
          if (product.colorList.length === 1) {
            selectedColor = product.colorNames?.[0] || product.colorList[0];
          }
          
          colorOptions.querySelectorAll('.color-opt').forEach(btn => {
            btn.addEventListener('click', () => {
              colorOptions.querySelectorAll('.color-opt').forEach(b => b.classList.remove('selected'));
              btn.classList.add('selected');
              selectedColor = (btn as HTMLElement).dataset.color || null;
              updateCartButton();
            });
          });
        } else {
          colorSection.style.display = 'none';
          selectedColor = 'none';
        }
      }

      // Stock status
      const stockStatus = document.getElementById('modalStockStatus');
      if (stockStatus) {
        stockStatus.className = `stock-info ${product.stockStatus}`;
        stockStatus.textContent = product.stockLabel;
      }

      updateCartButton();
      modal.classList.add('active');
      document.body.style.overflow = 'hidden';
    }

    function updateCartButton() {
      const btn = document.getElementById('addToCartBtn') as HTMLButtonElement;
      const btnText = btn?.querySelector('.btn-text');
      const btnPrice = btn?.querySelector('.btn-price');
      
      if (!btn || !currentProduct) return;

      const needsSize = currentProduct.sizes?.length > 0;
      const needsColor = currentProduct.colorList?.length > 0;
      const sizeOk = !needsSize || selectedSize;
      const colorOk = !needsColor || selectedColor;

      if (currentProduct.stockStatus === 'out-of-stock') {
        btn.disabled = true;
        if (btnText) btnText.textContent = 'Sold Out';
        if (btnPrice) btnPrice.textContent = '';
      } else if (!sizeOk || !colorOk) {
        btn.disabled = true;
        const missing = [];
        if (!sizeOk) missing.push('size');
        if (!colorOk) missing.push('colour');
        if (btnText) btnText.textContent = `Select ${missing.join(' & ')}`;
        if (btnPrice) btnPrice.textContent = '';
      } else {
        btn.disabled = false;
        if (btnText) btnText.textContent = 'Add to Cart';
        const price = currentProduct.onSale && currentProduct.salePrice 
          ? currentProduct.salePrice 
          : currentProduct.retailPrice;
        if (btnPrice) btnPrice.textContent = `£${price.toFixed(2)}`;
      }
    }

    // Add to Cart
    document.getElementById('addToCartBtn')?.addEventListener('click', () => {
      if (!currentProduct) return;
      
      const item = {
        id: currentProduct.id,
        name: currentProduct.name,
        type: 'merch',  // Important: identifies as physical merch not digital
        price: currentProduct.onSale && currentProduct.salePrice 
          ? currentProduct.salePrice 
          : currentProduct.retailPrice,
        image: currentProduct.imageUrl,
        size: selectedSize !== 'none' ? selectedSize : undefined,
        color: selectedColor !== 'none' ? selectedColor : undefined,
        quantity: 1
      };

      // Use global cart if available
      if (typeof (window as any).FreshWaxCart !== 'undefined') {
        (window as any).FreshWaxCart.add(item);
      } else {
        console.log('Added to cart:', item);
        alert(`Added ${item.name} to cart!`);
      }

      closeModal();
    });

    function closeModal() {
      modal?.classList.remove('active');
      document.body.style.overflow = '';
      currentProduct = null;
      selectedSize = null;
      selectedColor = null;
      
      // Remove hash from URL
      if (window.location.hash.startsWith('#quick-view-')) {
        history.pushState(null, '', window.location.pathname + window.location.search);
      }
    }
    
    // Handle browser back/forward navigation
    window.addEventListener('popstate', () => {
      const hash = window.location.hash;
      if (hash.startsWith('#quick-view-')) {
        const productId = hash.replace('#quick-view-', '');
        const products = (window as any).PRODUCTS_DATA || [];
        const product = products.find((p: any) => p.id === productId);
        if (product) {
          openModal(product, true); // skipPushState = true
        }
      } else {
        // Close modal if hash is gone
        modal?.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
    
    // Check for hash on page load
    const initialHash = window.location.hash;
    if (initialHash.startsWith('#quick-view-')) {
      const productId = initialHash.replace('#quick-view-', '');
      const products = (window as any).PRODUCTS_DATA || [];
      const product = products.find((p: any) => p.id === productId);
      if (product) {
        // Small delay to ensure DOM is ready
        setTimeout(() => openModal(product, true), 100);
      }
    }
  }

  document.addEventListener('DOMContentLoaded', init);
  document.addEventListener('astro:page-load', init);
</script>

